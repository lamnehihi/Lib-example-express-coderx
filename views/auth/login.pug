extends ../layouts/common
block content 
  h1 Login
  if errors
    each error in errors
      .alert.alert-danger(role='alert')=error
  form(action='/auth/login', method="post")
    .form-group
      label(for='title') Email:
      input#title.form-control(type='text', name='email', placeholder= "abc@x.x")
      small#emailHelp.form-text.text-muted email for test : aaass
    .form-group
      label(for='title') Password:
      input#title.form-control(type='password', name='password')
      small#emailHelp.form-text.text-muted password for test : 123123
    button.btn.btn-primary Login
  